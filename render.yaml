services:
  - type: web
    name: ai-phishing-detection-framework
    env: python
    buildCommand: pip install -r requirements.txt && python "Final Year project/phishing_detection_framework/model/train_model.py"
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
